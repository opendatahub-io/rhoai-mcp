apiVersion: apps/v1
kind: Deployment
metadata:
  name: rhoai-mcp
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: rhoai-mcp
          image: ghcr.io/opendatahub-io/rhoai-mcp:latest
