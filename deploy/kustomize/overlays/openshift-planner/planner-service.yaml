apiVersion: v1
kind: Service
metadata:
  name: llm-d-planner
  labels:
    app.kubernetes.io/name: llm-d-planner
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-d-planner
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
